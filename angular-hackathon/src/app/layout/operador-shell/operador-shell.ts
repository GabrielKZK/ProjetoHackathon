// Este layout foi migrado para o projeto angular-mobile-hackathon (app separado,
// dedicado ao fluxo do operador de chão de fábrica). Não é mais usado pelo
// angular-hackathon (gerenciador). Mantido apenas como referência histórica —
// pode ser apagado junto com esta pasta quando for conveniente.
export {};
